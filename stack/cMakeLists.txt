set(STACK_HEADERS
    stack.hpp
)

set(STACK_SOURCES
    stack.hpp
)

add_library(StackLib STATIC ${STACK_SOURCES} ${STACK_HEADERS})

target_include_directories(StackLib  PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})